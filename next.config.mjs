/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['i.imgur.com'], // Add any other domains you need
    },
  }
  
  export default nextConfig;
  